spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/users_db
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

jwt:
  secret: f136487323b05f5d608640747e52a4616cc5bb19198d1f414ddbd748268d32b9
  access-ttl-hours: 1
  refresh-ttl-days: 7

server:
  port: 8081

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE